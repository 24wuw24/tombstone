import Link from "next/link";
import { mockQuestions, categories } from "@/data/mockQuestions";

const difficultyStyles = { Beginner: "border-emerald-400/30 bg-emerald-400/10 text-emerald-300", Intermediate: "border-amber-400/30 bg-amber-400/10 text-amber-300", Advanced: "border-rose-400/30 bg-rose-400/10 text-rose-300" };

export default function Home() {
  return <main className="mx-auto min-h-screen max-w-6xl px-6 py-8 sm:px-10">
    <nav className="flex items-center justify-between border-b border-white/10 pb-6"><Link href="/" className="font-mono text-lg font-bold tracking-tight text-white">TOMBSTONE<span className="text-cyan-400">.</span></Link><span className="rounded-full border border-white/10 px-3 py-1 text-xs text-zinc-400">Open practice library</span></nav>
    <section className="grid gap-10 py-16 md:grid-cols-[1.3fr_0.7fr] md:items-end"><div><p className="mb-4 font-mono text-xs font-semibold tracking-[0.2em] text-cyan-400">FINANCE INTERVIEW DRILLS</p><h1 className="max-w-3xl text-5xl font-semibold tracking-tight text-white sm:text-6xl">Technical Interview Practice for High-Finance</h1><p className="mt-6 max-w-xl text-lg leading-8 text-zinc-400">Build the muscle memory to explain finance concepts clearly, logically, and under pressure.</p></div><div className="border-l border-cyan-400/40 pl-5 text-sm text-zinc-400"><span className="block text-3xl font-semibold text-white">{mockQuestions.length}</span>structured drills, scored against interview-ready milestones.</div></section>
    <section aria-labelledby="categories" className="mb-10"><h2 id="categories" className="mb-4 text-sm font-medium text-zinc-300">Browse by topic</h2><div className="flex flex-wrap gap-3">{categories.map((category) => <span key={category} className="rounded-full border border-white/10 bg-white/[0.03] px-4 py-2 text-sm text-zinc-300">{category}</span>)}</div></section>
    <section className="overflow-hidden rounded-xl border border-white/10 bg-zinc-950/60" aria-labelledby="questions"><div className="border-b border-white/10 px-5 py-4"><h2 id="questions" className="font-medium text-white">Available questions</h2></div><div className="divide-y divide-white/10">{mockQuestions.map((question) => <article key={question.id} className="flex flex-col gap-4 px-5 py-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-medium text-zinc-100">{question.title}</h3><div className="mt-2 flex flex-wrap items-center gap-2 text-xs"><span className="text-zinc-500">{question.category}</span><span className={`rounded border px-2 py-0.5 ${difficultyStyles[question.difficulty]}`}>{question.difficulty}</span></div></div><Link href={`/drill/${question.id}`} className="inline-flex shrink-0 items-center justify-center rounded-lg bg-cyan-400 px-4 py-2.5 text-sm font-semibold text-zinc-950 transition hover:bg-cyan-300">Practice →</Link></article>)}</div></section>
  </main>;
}
